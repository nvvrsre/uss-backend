name: Ushasree CI - Stage 0 (Code Checkout)

on:
  push:
    branches: [ main ]

jobs:
  checkout-code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
